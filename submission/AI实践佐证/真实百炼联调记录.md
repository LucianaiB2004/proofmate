# 真实百炼联调记录

- 日期：2026-09-25
- 接口：百炼 OpenAI-compatible Chat Completions
- 模型：`qwen-plus`
- 结果状态：`provider_ready`
- 输入：一段包含“7 天试点节能 31%，但缺少同周期对照”的项目陈述
- 输出：成功提取 2 条可验证主张，并识别“缺少对照组、因果可靠性不足”等风险
- 安全边界：API Key 仅保存在 Git 忽略的 `.env.local`，未进入源码、构建产物或提交包

联调过程中发现百炼 `json_object` 模式要求提示词显式包含小写 `json`，已增加回归测试并修正服务端适配器。
